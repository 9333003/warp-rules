# warp-rules

Утилита для серверов с Xray/VLESS: определяет, какие сервисы на сервере «сломаны» (определяются как Россия или заблокированы по региону), и выдаёт готовый блок маршрутизации для заворачивания их через WARP.

Построена как обёртка над [ipregion](https://github.com/vernette/ipregion).

## Что делает

1. Запускает `ipregion` и определяет **страну сервера по консенсусу** — берёт ответы всех GeoIP-сервисов и выбирает страну по большинству голосов (устойчиво к одиночным ошибкам отдельных баз).
2. Находит **сломанные сервисы** — те, что определяются как `RU` или имеют явный флаг недоступности (например `Gemini Supported: No`).
3. Проверяет **WARP-выход** — автоматически находит WireGuard/WARP-интерфейс и быстро определяет страну выхода через `cloudflare.com/cdn-cgi/trace` (без зависающего `ipregion -i`).
4. Выносит вердикт:
   - сервер не-RU, что-то сломано, WARP даёт нормальную страну → выдаёт готовый блок;
   - сервер RU, но WARP выходит в рабочую страну → всё равно чинит, выдаёт блок;
   - сервер RU и WARP тоже RU/отсутствует → сообщает «работа невозможна».
5. Печатает готовый JSON-блок доменов для секции `routing.rules`.

**YouTube не трогается никогда** — даже если определяется как RU. Это сделано намеренно: RU-выход для YouTube означает отсутствие рекламы, что является плюсом.

## Запуск

Одной командой на любом сервере (ничего ставить не надо — зависимости подтянутся):

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/9333003/warp-rules/main/warp-rules.sh)
```

Удобный алиас (выполнить один раз на каждом сервере):

```bash
echo "alias warp='bash <(curl -fsSL https://raw.githubusercontent.com/9333003/warp-rules/main/warp-rules.sh)'" >> ~/.bashrc
source ~/.bashrc
```

После этого достаточно набрать `warp`.

## Пример вывода

```
[*] Страна сервера (консенсус): NL  [голоса: NL×15 LT×2 ]
[!] Сломанные сервисы:
      - Google — определяется как RU
      - Gemini Supported — недоступен (No)
[*] Найден WARP-интерфейс: warp. Проверяю выход...
    WARP выходит как: DE  (дата-центр: FRA, IP: 104.28.x.x)
[OK] WARP-выход (DE) рабочий.
[*] Готовый блок для routing.rules (вставь ВЫШЕ дефолтного маршрута):
{
  "type": "field",
  "domain": [ "...домены..." ],
  "outboundTag": "warp-out"
}
```

## Как применить результат

Скопируй выданный JSON-блок и вставь в секцию `routing.rules` своего Xray-конфига **выше** дефолтного маршрута (и выше любого широкого правила, ловящего весь Google). Не забудь запятую между правилами. Перезапусти Xray и проверь:
- сломанный сервис (Gemini) теперь работает;
- YouTube по-прежнему без рекламы (остался на прямом выходе).

## Требования

- `curl` — для запросов;
- `jq` — для разбора JSON (ставится автоматически при первом запуске `ipregion`);
- `wireguard-tools` (`wg`) — для проверки WARP-выхода. Если отсутствует, скрипт выдаст блок, но пометит его как «предварительный, проверь WARP сам».

## Интерактивное меню

Кроме анализа сервера (описан выше), скрипт умеет запускаться без аргументов и показывать меню:

```
$ wrules

═══════════  warp-rules  ═══════════
  Нода Remnawave: 2.8.0 (xray 26.6.27)
  1. Анализ сервера + блок для конфига
  2. Проверка пригодности WARP (до установки)
  3. Установка инструментов (Remnawave / TrafficGuard / Решала / Multitest)
  4. Оптимизация (память, docker-лимиты, логи)
  5. Обновление системы + фикс при сбоях
  6. Обновление / откат ноды Remnawave
  7. Обновление / откат Xray-Core (без обновления ноды)
  0. Выход
════════════════════════════════════
```

### 6. Обновление / откат ноды Remnawave

Меняет тег образа `remnawave/node` в `docker-compose.yml` (только строку образа — env/volumes/порты не трогает), список версий подтягивается с GitHub. Перед правкой делает бэкап `docker-compose.yml`, валидирует конфиг через `docker compose config`, при любой ошибке — откатывает бэкап.

### 7. Обновление / откат Xray-Core (без обновления ноды)

Меняет **только бинарник `xray` внутри контейнера ноды** — версия образа Node не трогается. Полезно, когда нужна более новая (или, наоборот, конкретная старая) сборка Xray-Core без пересборки/обновления самой ноды.

```
[?] Выбор (0-7): 7

─── Обновление / откат Xray-Core (без обновления ноды) ───
  Контейнер:          remnanode
  Версия Xray-Core:   26.6.22
  Версия образа Node: 2.8.0
  Архитектура:        x86_64

  1. Выбрать версию Xray-Core (обновить/откатить)
  2. Показать список бэкапов и откатиться
  0. Отмена
─────────────────────────────────────────────────────────
[?] Выбор (0-2): 1

[*] Получаю список релизов Xray-Core с GitHub...

  1. v26.6.27
  2. v26.6.22
  3. v26.6.1
  4. v26.5.16
  5. v26.5.4
  6. v26.4.30
  7. v26.4.11
  8. v26.3.28
  9. Ввести версию вручную
  10. Показать все версии (включая пре-релизы)
  0. Отмена

[?] Выбор (0-10): 1
[*] Скачиваю Xray-linux-64.zip (v26.6.27)...
[✓] Бэкап текущей версии создан: /usr/local/bin/xray.bak-26.6.22
[*] Устанавливаю Xray-Core 26.6.27 в контейнер...
[✓] Xray-Core обновлён: 26.6.27

[!] Внимание: при следующем обновлении/пересоздании образа Node
    (пункт 6 этого меню или docker pull) версия Xray-Core будет
    перезаписана той, что зашита в новый образ Node. Если хочешь
    сохранить текущую версию Xray после обновления Node — повтори
    обновление Xray заново после пункта 6.
```

Проверка совместимости: на Node **>= 2.8.0** ставить Xray-Core **< 26.6.27** запрещено без явного подтверждения — скрипт покажет предупреждение `[!]` и потребует ввести фразу `да, я понимаю риск`, прежде чем продолжить. На Node младше 2.8.0 ограничений нет.

Если после установки версия внутри контейнера не совпадает с ожидаемой или в логах контейнера есть `fatal` — скрипт предложит автоматический откат на бэкап, сделанный перед обновлением. Пункт **2** меню («Показать список бэкапов») позволяет в любой момент откатиться на любой из ранее сохранённых бинарников (`/usr/local/bin/xray.bak-<версия>` внутри контейнера) без повторного скачивания.

## Настройка

В начале скрипта можно поменять:
- `WARP_TAG` — тег outbound, в который заворачивается трафик (по умолчанию `warp-out`);
- `BAD_COUNTRIES` — список «нерабочих» стран выхода (RU, CN и т.д.);
- `SKIP_SERVICES` — сервисы, которые никогда не трогать (по умолчанию YouTube);
- `SERVICE_DOMAINS` — таблица соответствия «сервис → домены/geosite».
